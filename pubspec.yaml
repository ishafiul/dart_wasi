name: dart_wasi_workspace
description: Experiments compiling Dart to WASI and hosting it from Dart.
publish_to: none

environment:
  sdk: ^3.11.0

workspace:
  - packages/dart2wasi
  - packages/dart_wasi
  - packages/wasi_runtime

dependencies:
  wasi_runtime: any

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
