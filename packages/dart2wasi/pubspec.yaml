name: dart2wasi
description: Experimental compiler research for producing WASI modules from Dart.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
